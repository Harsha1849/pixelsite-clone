import React from "react";
import "../components/CartPage.css";

const CartPage = () => {
  return (
    <div className="cart-page">
      <h1>Your Cart</h1>
      <p>Your cart is currently empty. Start adding products!</p>
      {/* Add logic here to display cart items */}
    </div>
  );
};

export default CartPage;
